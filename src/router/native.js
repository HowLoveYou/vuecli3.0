import {
    _import
} from '../utils/common' //模块加载方法
const nativejs = [
	{
        path: "clone",
        name: "clone",
        component: _import('js/components/clone')
    },
]
export default nativejs
